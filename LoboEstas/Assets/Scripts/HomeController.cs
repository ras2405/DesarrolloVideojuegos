using UnityEngine;
using TMPro;

public class HomeController : MonoBehaviour
{
   /* public void ChangeDay(int newDay)
    {
        // Cambia el día en CycleDayController
        CycleDayController.Instance.SetDayAndTime(newDay, CycleDayController.Instance.GameTimeInMinutes);
    }

    public void ChangeTime(float newTimeInMinutes)
    {
        // Cambia el tiempo en CycleDayController
        CycleDayController.Instance.SetDayAndTime(CycleDayController.Instance.CurrentDay, newTimeInMinutes);
    }

    public void DisplayCurrentTime(TextMeshProUGUI timeText)
    {
        timeText.text = $"Día {CycleDayController.Instance.CurrentDay}, {CycleDayController.Instance.GameTimeInMinutes / 60:D2}:{CycleDayController.Instance.GameTimeInMinutes % 60:D2}";
    }*/
}